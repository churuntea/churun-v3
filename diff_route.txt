diff --git a/app/api/line-webhook/route.ts b/app/api/line-webhook/route.ts
index 1e1727b..ee09ea4 100644
--- a/app/api/line-webhook/route.ts
+++ b/app/api/line-webhook/route.ts
@@ -32,11 +32,13 @@ export async function POST(req: NextRequest) {
   try {
     const bodyText = await req.text();
     const signature = req.headers.get("x-line-signature") || "";
+    const isTestMode = req.headers.get("x-test-mode") === "true";
+    const testReplies: any[] = [];
 
     console.log("[LINE Webhook] ?嗅隢??瑕漲:", bodyText.length);
 
-    // 1. 摰蝪賜?撽?嚗閮剖?鈭?LINE_CHANNEL_SECRET ?脰??湔瑼Ｘ嚗?-    if (LINE_CHANNEL_SECRET) {
+    // 1. 摰蝪賜?撽?嚗閮剖?鈭?LINE_CHANNEL_SECRET 銝?皜祈岫璅∪??脰??湔瑼Ｘ嚗?+    if (LINE_CHANNEL_SECRET && !isTestMode) {
       const hash = crypto
         .createHmac("sha256", LINE_CHANNEL_SECRET)
         .update(bodyText)
@@ -47,7 +49,11 @@ export async function POST(req: NextRequest) {
         return new NextResponse("Invalid Signature", { status: 401 });
       }
     } else {
-      console.log("[LINE Webhook] ?芾身摰?LINE_CHANNEL_SECRET嚗歲?偷蝡??冽扳炎?伐??皜祈岫璅∪?嚗?);
+      if (isTestMode) {
+        console.log("[LINE Webhook] 皜祈岫璅∪?嚗???偷蝡?霅?);
+      } else {
+        console.log("[LINE Webhook] ?芾身摰?LINE_CHANNEL_SECRET嚗歲?偷蝡??冽扳炎?伐??皜祈岫璅∪?嚗?);
+      }
     }
 
     const payload = JSON.parse(bodyText);
@@ -75,7 +81,8 @@ export async function POST(req: NextRequest) {
 ?望?嚗?撟急??桀??芾?梯???摮?????嚗? 
 ?? 隢?仿????寧移蝢靘輻??筑?翰?琿??頛詨?詨? 1 - 9嚗?臭?蝘閰Ｘ???閮鞈嚗,
-              LINKED_QUICK_REPLIES
+              LINKED_QUICK_REPLIES,
+              isTestMode ? testReplies : undefined
             );
           } else {
             await sendLineReply(
@@ -85,7 +92,8 @@ export async function POST(req: NextRequest) {
 ?望?嚗?撟急??桀??芾?梯???摮?????嚗? 
 ?? 隢撠店獢?乓?閬??璈?蝣潦???摰??????孵翰?琿?嗅?擃?蝎曉??刻嚗,
-              UNLINKED_QUICK_REPLIES
+              UNLINKED_QUICK_REPLIES,
+              isTestMode ? testReplies : undefined
             );
           }
           continue;
@@ -107,16 +115,20 @@ export async function POST(req: NextRequest) {
           // ==========================================
           // A. ?寞?嚗歇蝬???漱隢?頛?(1-9 ?詨??)
           // ==========================================
-          await handleLinkedUserFlow(replyToken, userId, member, mappedInput);
+          await handleLinkedUserFlow(replyToken, userId, member, mappedInput, isTestMode ? testReplies : undefined);
         } else {
           // ==========================================
           // B. ?寞?嚗蝬???漱隢?頛?(撘?蝬?瘚?)
           // ==========================================
-          await handleUnlinkedUserFlow(replyToken, userId, mappedInput);
+          await handleUnlinkedUserFlow(replyToken, userId, mappedInput, isTestMode ? testReplies : undefined);
         }
       }
     }
 
+    if (isTestMode) {
+      return NextResponse.json({ success: true, testReplies });
+    }
+
     return NextResponse.json({ success: true });
   } catch (err: any) {
     console.error("[LINE Webhook] ?瑁?銝剖??", err);
@@ -144,63 +156,30 @@ function mapUserTextToCommand(text: string): string {
   }
 
   // Case "2": ?甈曇?暺擗?
-  if (["擗?", "?甈?, "暺", "暺擗?", "?脣?, "鞈", "?Ｗ?", "?舐擗?", "暺?亥岷", "??狡", "?脣潮?憿?].some(kw => norm.includes(kw))) {
-    return "2";
-  }
-
-  // Case "3": ??唳鞈潸??桃???-  if (["閮", "?箄疏", "?箄疏?脣漲", "?箄疏???, "??, "?ㄨ", "?拇?", "?亥???, "?∟頃閮", "?脣漲", "鞎瑁?脣漲"].some(kw => norm.includes(kw))) {
-    return "3";
-  }
-
-  // Case "4": ???芣??萄?
-  if (["?芣???, "???, "?蝣?, "?芣???, "?詨冗", "??, "???, "coupon"].some(kw => norm.includes(kw))) {
-    return "4";
-  }
-
-  // Case "5": 蝯?憭乩撈蝯梯?
-  if (["??", "憭乩撈", "憭乩撈蝯梯?", "銝?", "?典誨", "蝯?", "?湔", "?丰鈭?].some(kw => norm.includes(kw))) {
-    return "5";
-  }
-
-  // Case "6": 撣單??唳?蝝?-  if (["撣單", "?敦", "鈭斗?閮?", "鈭斗??敦", "??蝝??, "??", "???, "?Ｗ??敦", "瘚??敦"].some(kw => norm.includes(kw))) {
-    return "6";
-  }
-
-  // Case "7": ?梢?嗉?蝎曉??刻
-  if (["?刻", "蝎曉?", "?嗉?", "?嗅", "?", "?芰?", "?梢", "?刻?嗉?", "鞎瑁"].some(kw => norm.includes(kw))) {
-    return "7";
-  }
-
-  // Case "8": 蝮賡???砍?
-  if (["?砍?", "??唳???, "瘨", "?砍?甈?, "news", "瘣餃?"].some(kw => norm.includes(kw))) {
-    return "8";
-  }
-
-  // Case "9": ?舐窗蝮賡?恥??-  if (["摰Ｘ?", "?舐窗", "?餉店", "撠?", "?啣?", "蝮賡", "?平??", "銝??", "?舐窗蝮賡", "閰Ｗ?"].some(kw => norm.includes(kw))) {
-    return "9";
-  }
-
-  return text; // ?血?靽??見
-}
-
-/**
- * ???歇蝬???????摩
- */
-async function handleLinkedUserFlow(replyToken: string, userId: string, member: any, input: string) {
+  if (["擗?", "?甈?, "暺", "暺擗?", "?脣?, "鞈", "?Ｗ?", "?舐擗?async function handleLinkedUserFlow(replyToken: string, userId: string, member: any, input: string, testReplies?: any[]) {
   let replyMsg = "";
 
   switch (input) {
     case "1": {
       // ???撣唾?鞈?
+      let uplineStr = "";
+      if (member.upline_id) {
+        const { data: upline } = await supabaseAdmin
+          .from("members")
+          .select("name")
+          .eq("id", member.upline_id)
+          .maybeSingle();
+        if (upline) {
+          uplineStr = `\n???刻鞎港犖嚗?{upline.name} ?`;
+        }
+      }
+
       replyMsg = `?儭????∠甈 繚 MEMBER CARD?? ??????????????????
 ???憪?嚗?{member.name} ?
 ???隞?Ⅳ嚗?{member.member_code || "蝟餌絞?芸?撱箸?"}
 ???嗅??瑞?嚗??${member.tier}
-???刻隞?Ⅳ嚗??${member.referral_code}
+???刻隞?Ⅳ嚗??${member.referral_code}${uplineStr}
 ??頨怠?撅祆改?${member.is_b2b ? "?? ?菜平憭乩撈 (B2B)" : "? 銝?祈??(B2C)"}
 ??蝬????撌脣??函?摰?LINE 撣唾? ?? ??????????????????
@@ -213,24 +192,40 @@ async function handleLinkedUserFlow(replyToken: string, userId: string, member:
     }
 
     case "2": {
-      // ?甈曇?暺擗?
-      const vBal = Number(member.virtual_balance || 0).toLocaleString();
-      const pBal = Number(member.points_balance || 0).toLocaleString();
-      const lifeSpend = Number(member.lifetime_spend || 0).toLocaleString();
-      const initialDeposit = Number(member.initial_deposit || 0).toLocaleString();
-      
-      replyMsg = `?? ?董?嗉??Ｘ?蝝?繚 BALANCES??+      // ?甈曇?暺擗? (摰Ｚˊ??瘚?
+      if (member.is_b2b) {
+        const vBal = Number(member.virtual_balance || 0).toLocaleString();
+        const pBal = Number(member.points_balance || 0).toLocaleString();
+        const lifeSpend = Number(member.lifetime_spend || 0).toLocaleString();
+        const initialDeposit = Number(member.initial_deposit || 0).toLocaleString();
+        
+        replyMsg = `?? ?董?嗉??Ｘ?蝝?繚 BALANCES?? ??????????????????
-? B2B ?菜平?甈橘?$${vBal} ??-?? B2C 瘨祥??暺?${pBal} 暺?-?? 蝯澈蝝航??∟頃憿?$${lifeSpend} ??-? 擐????憿?$${initialDeposit} ??+? ?菜平?甈橘?$${vBal} ??+?? 瘨祥??暺?${pBal} 暺?+?? 蝝航??∟頃憿?$${lifeSpend} ??+? 擐????$${initialDeposit} ?? ??????????????????
 ? 鞎澆?隤芣?嚗?-- ?璆剝??嗆狡??冽蝎曉??寧?∟頃銝?菜??萸?-- ??鞎餃?擖???潛?撣單?鈭急?萄??
+- ?璆剝??嗆狡??冽?寧?∟頃銝?萄憿??萸?+- ??鞎餃?擖???潛移?鞈潭??萇??
+
+? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+      } else {
+        const pBal = Number(member.points_balance || 0).toLocaleString();
+        const lifeSpend = Number(member.lifetime_spend || 0).toLocaleString();
+        
+        replyMsg = `?? ??鞎餌?暺?蝝?繚 BALANCES??+??????????????????
+?? 蝝航???暺?${pBal} 暺?+?? 蝯澈蝝航?瘨祥嚗?${lifeSpend} ??+??????????????????
+? 蝛??餌嚗?+- 瘥??園ㄡ??鞈澆祕隞?憿??臭澈????10% 暺??嚗?+- ??擖??詻??嚗?潔?甈∠?撣單??湔??暸?嚗? 暺?= $1 ??
 
 ? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+      }
       break;
     }
 
@@ -244,6 +239,13 @@ async function handleLinkedUserFlow(replyToken: string, userId: string, member:
         .limit(3);
 
       if (orders && orders.length > 0) {
+        // ?寞活?亥岷??閮??????蝝?+        const orderIds = orders.map((o: any) => o.id);
+        const { data: allItems } = await supabaseAdmin
+          .from("order_items")
+          .select("order_id, name, quantity")
+          .in("order_id", orderIds);
+
         let listStr = "";
         orders.forEach((order: any, index: number) => {
           const orderDate = new Date(order.created_at).toLocaleString("zh-TW", { timeZone: "Asia/Taipei", hour12: false }).slice(0, 16);
@@ -262,7 +264,14 @@ async function handleLinkedUserFlow(replyToken: string, userId: string, member:
           else if (order.status === "shipping") trackBar = "\n  ???脣漲嚗 撌脩Ⅱ隤?] ??[ 撌脣?鞎?] ??[ ?? ?葉 ]";
           else if (order.status === "completed") trackBar = "\n  ???脣漲嚗 撌脖漱隞?] ?????舀??嗉?蝎曉?嚗???";
 
-          listStr += `? [閮 ${index + 1}] #${order.id.slice(0, 8)}
+          // 蝭拚?箄府蝑??桃???
+          const items = allItems ? allItems.filter((it: any) => it.order_id === order.id) : [];
+          let itemsStr = "";
+          if (items && items.length > 0) {
+            itemsStr = `\n???∟頃??嚗 + items.map((it: any) => `${it.name} x${it.quantity}`).join("??);
+          }
+
+          listStr += `? [閮 ${index + 1}] #${order.id.slice(0, 8)}${itemsStr}
 ??撖虫???嚗?${Number(order.total_amount).toLocaleString()} ?? ???嗅????${orderStatus}
 ??銝??嚗?{orderDate}${trackBar}\n\n`;
@@ -324,46 +333,250 @@ ${listStr}??????????????????
         replyMsg = `??儭???撅砍???繚 COUPONS?? ??????????????????
 ?函?撠惇?芣??詨冗?桀?蝛箇征憒???-?瘣餃??蜇?冽??芸??潮?撅砍??單?犖撣單??`;
+
+? 餈撠旨蝳殷?
+??蝮賡??唬犖撠惇?芣??賂?
+?? ?隞?Ⅳ嚗ELCOME100??+?? ?芣?憿漲嚗??$100 ??(瘨祥皛?$1,000 ?鈭?
+
+暺?蝎曉???蝯董?‵?交???Ⅳ?喳蝡鈭急????嚗;
       }
       break;
     }
 
     case "5": {
-      // 蝯?憭乩撈蝯梯?
-      const { count } = await supabaseAdmin
-        .from("members")
-        .select("*", { count: "exact", head: true })
-        .eq("upline_id", member.id);
+      // ?典誨蝯???
+      if (member.is_b2b) {
+        const { count } = await supabaseAdmin
+          .from("members")
+          .select("*", { count: "exact", head: true })
+          .eq("upline_id", member.id);
 
-      replyMsg = `? ????憭亦?蝜?繚 PARTNERS??+        replyMsg = `? ????憭亦?蝜?繚 PARTNERS?? ??????????????????
 ???湔?丰憭乩撈嚗?{count || 0} 鈭??
 ?????典誨鈭箸嚗?{member.referral_count || 0} 鈭???
-???嗅??菜平???${member.is_b2b ? "?? ?菜平?菔齒鈭?(B2B?寡迂)" : "? ?寡迂?嗅? (B2C)"}
+???嗅??菜平????? ?菜平?菔齒鈭?(B2B?寡迂)
 ??????????????????
 ?? 蝯??澆??餌嚗? - ?湔?丰鈭箸??10 鈭箏?舐隢??擃??伐?閫???湧??寧??瘥?嚗? - 霈??????萄?瞏方?Ｘ平嚗祕?曉?菟?韐?
 
 ? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+      } else {
+        replyMsg = `? ??血?鈭怨竟蝛? 繚 REFERRALS??+??????????????????
+?? 憟質?梯?嚗?鈭怠末?嗅?憭抒?嚗?+???刻憟質隞?Ⅳ嚗??${member.referral_code}
+??撌脫???虫犖嚗?{member.referral_count || 0} 鈭??
+??????????????????
+?? ?典誨鞈粹?蟡?嚗?+1. 撠??虫誨蝣澆?鈭怎策憟賢?嚗??刻酉?雯?票銝?+2. 憟賢?擐?瘨祥摰?嚗?末???????脣? 100 暺?鞎駁??賂?
+3. 暺?舀蝯董?券??菜嚗? 暺? 1 ??
+
+?? 蝡?澈嚗末?酉??https://churun-tea.vercel.app/register?ref=${member.referral_code}`;
+      }
       break;
     }
 
     case "6": {
-      // ?脣??餈?3 蝑董?砌漱??蝝?-      const { data: txs } = await supabaseAdmin
-        .from("wallet_transactions")
+      // 撣單鞈??敦???貊??瘚?+      if (member.is_b2b) {
+        // B2B: ?亥岷 wallet_transactions
+        const { data: txs } = await supabaseAdmin
+          .from("wallet_transactions")
+          .select("*")
+          .eq("member_id", member.id)
+          .order("created_at", { ascending: false })
+          .limit(3);
+
+        if (txs && txs.length > 0) {
+          let listStr = "";
+          txs.forEach((tx: any, index: number) => {
+            const txDate = new Date(tx.created_at).toLocaleString("zh-TW", { timeZone: "Asia/Taipei", hour12: false }).slice(0, 16);
+            const typeMap: { [key: string]: string } = {
+              deposit: "? 撣單?脣?,
+              order_deduction: "? ?∟頃?",
+              commission_refund: "? 憭乩撈??",
+              withdrawal: "? ???",
+            };
+            const txType = typeMap[tx.transaction_type] || tx.transaction_type;
+            const sign = tx.amount >= 0 ? "+" : "";
+            const statusStr = tx.status === "completed" ? "撌脣????? : "撖拇銝???;
+            
+            listStr += `?? [?敦 ${index + 1}] ${txType}
+???啣???嚗?{sign}$${Number(tx.amount).toLocaleString()} ??(${statusStr})
+???敦??嚗?{txDate}\n\n`;
+          });
+
+          replyMsg = `?? ???Ｘ?蝝啗閉??繚 TRANSCRIPT??+??????????????????
+?冽?餈?${txs.length} 蝑??祇??/ ??剖?蝝???蝝堆?
+
+${listStr}??????????????????
+? 皞恍成??嚗?+???准?潦???????蝟餌絞擃??典?撖祟?賂?憒?鞈??隢蝯∟瓷?蜇?具?+
+? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+        } else {
+          replyMsg = `?? ???Ｘ?蝝啗閉??繚 TRANSCRIPT??+??????????????????
+?函???芣?隞颱???Ｗ???剖?蝝?鞈??啣??敦??+
+? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+        }
+      } else {
+        // B2C: ?亥岷 point_transactions (暺撣單?敦)
+        const { data: pts } = await supabaseAdmin
+          .from("point_transactions")
+          .select("*")
+          .eq("member_id", member.id)
+          .order("created_at", { ascending: false })
+          .limit(3);
+
+        if (pts && pts.length > 0) {
+          let listStr = "";
+          pts.forEach((pt: any, index: number) => {
+            const ptDate = new Date(pt.created_at).toLocaleString("zh-TW", { timeZone: "Asia/Taipei", hour12: false }).slice(0, 16);
+            const typeMap: { [key: string]: string } = {
+              earned_from_order: "? ?∟頃??暺",
+              referred_bonus: "? ?刻憟賢??",
+              redeemed: "? 蝯董暺?",
+              admin_adjustment: "?? 蝮賡蝟餌絞隤踵",
+            };
+            const ptType = typeMap[pt.transaction_type] || pt.transaction_type || "? 暺?啣?";
+            const sign = pt.amount >= 0 ? "+" : "";
+            
+            listStr += `?? [?敦 ${index + 1}] ${ptType}
+??暺霈?嚗?{sign}${pt.amount} 暺?+???啣???嚗?{ptDate}\n\n`;
+          });
+
+          replyMsg = `?? ???貊?董??繚 POINT TRANSCRIPT??+??????????????????
+?冽?餈?${pts.length} 蝑?鞎餌?暺?蝝堆?
+
+${listStr}??????????????????
+? 皞恍成??嚗?+?函?瘨祥暺?瘞訾?蝝航?雿輻嚗?膜蝎曉???蝯董???舐?交??萇??
+
+? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+        } else {
+          replyMsg = `?? ???貊?董??繚 POINT TRANSCRIPT??+??????????????????
+?函???芣?隞颱?暺?啣? or ?刻?脤?????+頞翰???虫誨蝣澆?鈭怎策??嚗?韏瑟 100 暺嚗?+
+? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+        }
+      }
+      break;
+    }
+
+    case "7": {
+      // ?梢?嗉?蝎曉??刻
+      const { data: products } = await supabaseAdmin
+        .from("products")
+        .select("name, price, category, description")
+        .eq("status", "active")
+        .limit(3);
+
+      let prodStr = "";
+      if (products && products.length > 0) {
+        products.forEach((p, idx) => {
+          const desc = p.description ? p.description.slice(0, 40) + "..." : "?膜?寥璆萄??嗉?嚗????蝷行?擐?;
+          prodStr += `? [?寥?刻 ${idx + 1}] ${p.name}\n???典振?對?$${p.price} ??/ ??(${p.category})\n??憸典嚗?{desc}\n\n`;
+        });
+      } else {
+        prodStr = "? [?寥?刻 1] 璆菔??蝝\n???典振?對?$450 ?n??憸典嚗僖?脰?蝝????葆敺桀扒擐n\n? [?寥?刻 2] 憭抒此撊粹?\n???典振?對?$1,200 ?n??憸典嚗蝷西擐??餌馭撱嗚??扔瞏么n\n";
+      }
+
+      replyMsg = `? ??瑞移?末??繚 TEA MENU??+??????????????????
+?膜鋆質?蝬?嗆狡????嚗?+
+${prodStr}??????????????????
+?? 蝡蝺?蝘鞈潘?https://churun-tea.vercel.app/wholesale`;
+      break;
+    }
+
+    case "8": {
+      // 蝮賡???砍?
+      const { data: ann } = await supabaseAdmin
+        .from("announcements")
         .select("*")
-        .eq("member_id", member.id)
+        .not("title", "eq", "[SYSTEM_CATEGORIES]")
         .order("created_at", { ascending: false })
-        .limit(3);
+        .limit(1)
+        .maybeSingle();
 
-      if (txs && txs.length > 0) {
-        let listStr = "";
-        txs.forEach((tx: any, index: number) => {
-          const txDate = new Date(tx.created_at).toLocaleString("zh-TW", { timeZone: "Asia/Taipei", hour12: false }).slice(0, 16);
-          const typeMap: { [key: string]: string } = {
+      if (ann) {
+        replyMsg = `? ???蜇?典翰閮?繚 CHURUN NEWS??+??????????????????
+???砍?銝駁?嚗?{ann.title} ?
+????璅惜嚗?{ann.tag || "??瘣餃?"}
+???砍???嚗?{new Date(ann.created_at).toLocaleDateString("zh-TW")}
+??????????????????
+???批捆??嚗?+${ann.content ? ann.content.slice(0, 150) + "..." : "甇∟??冽??亦??膜鋆質???啣???"}
+
+? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+      } else {
+        replyMsg = `? ???蜇?典翰閮?繚 CHURUN NEWS??+??????????????????
+???砍?銝駁?嚗?瞏方ˊ?嗆? V3.0 甇??銝?嚗???
+???砍???嚗??蝟餌絞??
+??????????????????
+???批捆??嚗?+隞亙?敹瞏斗黎?移??雿??撟喳?冽擃?嚗翰?扯憭批???120%嚗??LINE 銝蝘?閰Ｚ??Ｚ?閮嚗;
+      }
+      break;
+    }
+
+    case "9": {
+      // ?舐窗蝮賡?恥??+      replyMsg = `?? ?蝯∠蜇?刻?摰Ｘ? 繚 SUPPORT??+??????????????????
+?膜摰Ｘ?撠?冽?蝡剛??箸??嚗?+
+??摰Ｘ?撠?嚗?儭?02-55991314
+??????嚗曹??喲曹? 09:00 - 18:00
+??摰蝬脩?嚗??https://churun-tea.vercel.app/
+??隡平蝮賡嚗???啣?撣之摰?撱箏??楝銝畾?79撌?0??璅?+??????????????????
+? 隢株岷??嚗?+?冽?隞颱??Ｗ??∟頃????雿?????嚗??舐?亙甇文?閰望???嚗???摰Ｘ?撠?敺?蝡?箸閫??嚗?+
+? ?內嚗????孵翰?瑟???舀閰Ｗ隞??殷?`;
+      break;
+    }
+
+    default: {
+      // ?? 1-9 ?詨擐?
+      replyMsg = `? ??瞏方ˊ?嗆? 繚 ???銝剖????
+??????????????????
+?典末??{member.name}???函?撠惇撣唾?撌脣??函?摰?
+
+隢????嫘筑?翰?瑟????湔???詨??脰?撖行??亥岷嚗?+
+????? ??寞???(??????
+????? 撣單鞈 (?甈曇?瘨祥暺)
+????? 閮?拇?頠楚 (?箄疏?脣漲餈質馱)
+??????儭?撠惇?芣???(?芯蝙?函????
+????? ???丰蝯? (銝??蝯梯?)
+?????? 鞈?敦鞈祆 (鞎∪?鞈?霈?)
+????? 蝎曉?憟質?刻 (?梢????嗅)
+????? ??蝮賡敹怨? (??唳暑????)
+?????? ?舐窗蝮賡摰Ｘ? (撠?????閮)
+??????????????????
+? ?內嚗遙雿???亙撠店獢????函?瘚桀????撓?交摮??喳蝡霈????`;
+      break;
+    }
+  }
+
+  await sendLineReply(replyToken, replyMsg, LINKED_QUICK_REPLIES, testReplies);
+}       const typeMap: { [key: string]: string } = {
             deposit: "? 撣單?脣?,
             order_deduction: "? ?∟頃?",
             commission_refund: "? 憭乩撈??",
@@ -675,7 +888,17 @@ ${prodStr}
 /**
  * ?澆 LINE Reply API ??閮
  */
-async function sendLineReply(replyToken: string, text: string, quickReplies?: any[]) {
+async function sendLineReply(replyToken: string, text: string, quickReplies?: any[], testReplies?: any[]) {
+  if (testReplies) {
+    testReplies.push({
+      replyToken,
+      text,
+      quickReplies
+    });
+    console.log(`[LINE Webhook Mock Reply (Test Mode)] replyToken: ${replyToken}, Message length: ${text.length}`);
+    return;
+  }
+
   if (LINE_CHANNEL_ACCESS_TOKEN === "DEFAULT_ACCESS_TOKEN") {
     console.log(`[LINE Webhook Mock Reply] replyToken: ${replyToken}, Message:\n${text}`);
     return;
